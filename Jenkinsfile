pipeline {
    agent any
       stages {
        stage('Test...') {
            steps {
                sh 'python --version'
            }
        }
        stage('build...') {
            steps {
                sh 'python manage.py'
            }
        }
    }
}
